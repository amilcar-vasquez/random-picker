Test #2: Random Image + Random Quote Poster

This worksheet  guides you through the required steps to complete the test.  Fill in each section as you work.
1. Default Content
Write your chosen default image URL:
https://picsum.photos/800/600
Write your chosen default quote:
"The only way to do great work is to love what you do." - Steve Jobs
2. HTML Structure
Describe the elements you will include (poster area, button, status message):
- A <div id="poster"> container with position: relative
- An <img id="posterImage"> for the background image
- A <div id="posterQuote"> overlaid on the image for the quote text
- A <button id="generateBtn"> to trigger new content fetching
- A <div id="status"> to display loading/error messages
3. CSS Overlay Strategy
Explain how you will place the quote on top of the image:
- Set #poster to position: relative
- Set #posterQuote to position: absolute with bottom/left positioning
- Add semi-transparent background (e.g., rgba(0,0,0,0.6)) for readability
- Use white text with padding and appropriate font styling
- Center or position the quote for visual appeal
4. Fetch Logic Plan
What API endpoints will you call?
Image API: https://picsum.photos/800/600 (Lorem Picsum - no key required)
Quote API: https://api.quotable.io/random (Quotable - no key required)
Describe the steps your fetch logic will follow:
1. Display "Loading..." in status message
2. Use Promise.all() to fetch both APIs simultaneously
3. Parse the quote API JSON response to get quote text and author
4. Update the image src attribute with the new image URL
5. Update the quote div with formatted quote text
6. Clear status message or show "Success!"
7. Handle any errors and fall back to defaults if needed
5. Error Handling Plan
Explain how your page will behave if:
- Image fetch fails: Keep the current/default image, update quote if successful, show error in status
- Quote fetch fails: Update image if successful, keep the current/default quote, show error in status
- Both fail: Keep all current/default content, display error message: "Failed to fetch new content. Please try again."
6. Final Verification
Checklist:
- Default content shows on page load
- Button fetches new image + quote
- Status message updates
- Poster updates or falls back to defaults
- No blank UI states


